target-version = "py312"
line-length = 100

[lint]
select = ["E", "W", "F", "I", "B", "C4", "UP"]
ignore = [
    "E501",
    "B008",  # FastAPI Depends() in defaults
]
per-file-ignores = { "tests/**" = ["S", "ANN"] }

[format]
quote-style = "double"
indent-style = "space"
